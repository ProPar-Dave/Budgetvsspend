You are still in STRICT EXECUTION MODE.

Do not make assumptions.
Do not change calculations.
Do not change values.
Do not change option sets.
Do not change breadcrumb behavior.
Do not change table behavior.

OBJECTIVE

Align the top of the page to the provided target design.

The target design has two clearly separated layers:

1. Context controls
2. Details

The context controls are compact selectors.
The details layer contains only breadcrumb and scope.

Follow the structure and hierarchy below exactly.

TOP-OF-PAGE STRUCTURE TO MATCH

Row 1: Context controls
- Timeframe
- Metric
- Spend Mode
- Table Size

Row 2: Details
- breadcrumb
- scope line

No other elements should appear in this area.

CONTEXT ROW REQUIREMENTS

1. LEFT GROUP = PRIMARY REPORT CONTEXT

On the left side of the row, group these three controls together:

- Timeframe
- Metric
- Spend Mode

They must:
- appear as compact select-style controls
- each have a visible label above the selected value
- use the same control pattern and same visual sizing
- read as one unified analytical context group

2. RIGHT GROUP = SECONDARY SUPPORT CONTROL

On the far right side of the same row, place:

- Table Size

This control should:
- use the same select-style pattern
- remain visually secondary to the three primary context controls
- be clearly separated from the primary context group

3. DO NOT INCLUDE FACILITY SCOPE

Do not reintroduce Facility Scope.
The viewed object is already represented by the breadcrumb/details layer.

4. COMPACT SIZING

The context controls should be smaller and tighter than the previous oversized card-like controls.

Requirements:
- compact vertical height
- label visible and subordinate
- selected value primary and readable
- no oversized box treatment
- no extra padding that makes the row feel heavy

5. SELECT-STYLE INTERACTION MODEL

The controls should follow an explicit selection pattern, not cycling.

Use the existing option sets and current behavior model already established.

DETAILS ROW REQUIREMENTS

1. BREADCRUMB FIRST

Directly beneath the context row, place the breadcrumb as the primary detail line.

2. SCOPE SECOND

Place the scope line directly beneath the breadcrumb.

3. DETAILS ROW SHOULD CONTAIN ONLY:
- breadcrumb
- scope

Do not add:
- Current View
- KPI Scope
- Values reflect
- Report Context
- reconciliation line
- helper text

HIERARCHY REQUIREMENTS

The page must now read top-to-bottom as:

- Context row
- Details row
- KPI summary
- Table

This hierarchy should be visually obvious through spacing and grouping.

LAYOUT REQUIREMENTS

1. CONTEXT ROW

- primary context controls grouped on the left
- secondary table size control on the right
- both aligned on the same row
- clean horizontal balance
- no wasted oversized control containers

2. DETAILS ROW

- breadcrumb left aligned beneath context row
- scope line left aligned directly beneath breadcrumb
- calm and compact
- clearly separated from context row above and KPI row below

3. REMOVE EXCESS VERTICAL WEIGHT

The top of the page should feel efficient and intentional.
Do not preserve unnecessary vertical bulk from the earlier design.

DO NOT CHANGE

Do NOT change:
- KPI values
- KPI layout
- breadcrumb wording or click behavior
- scope text
- table
- row emphasis
- calculations
- page logic

OUTPUT

After implementing, return:

1. CONTEXT ROW RESTRUCTURED TO MATCH TARGET
2. PRIMARY CONTEXT CONTROLS GROUPED LEFT
3. TABLE SIZE CONTROL MOVED TO SECONDARY POSITION ON RIGHT
4. DETAILS ROW LIMITED TO BREADCRUMB + SCOPE
5. TOP-TO-BOTTOM HIERARCHY MATCHES TARGET
6. CONFIRM NO OTHER CHANGES

Do not propose next steps.
Stop after reporting changes.